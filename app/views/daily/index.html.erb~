<h1>Listing Shifts For the Next Week</h1>
<a href="calendar" target="calendar">Back to Calendar</a>
<table>
  <tr>
    <th>Name</th>
    <th>Start at</th>
    <th>End at</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<h2><%= @date.strftime("%A") %></h2>
<% @events.each do |event| %>
  <tr>
    <td><%= event.name %></td>
    <td><%= event.start_at %></td>
    <td><%= event.end_at %></td>
    <td><%= link_to 'Show', event %></td>
  </tr>
<% end %>
</table>

<table>
  <tr>
    <th>Name</th>
    <th>Start at</th>
    <th>End at</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>


<h2><%= (@start_date +1).strftime("%A") %></h2>
<% @events2.each do |event| %>
  <tr>
    <td><%= event.name %></td>
    <td><%= event.start_at %></td>
    <td><%= event.end_at %></td>
    <td><%= link_to 'Show', event %></td>
  </tr>
<% end %>

</table>

<table>
  <tr>
    <th>Name</th>
    <th>Start at</th>
    <th>End at</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>


<h2><%= (@start_date +2).strftime("%A") %></h2>
<% @events3.each do |event| %>
  <tr>
    <td><%= event.name %></td>
    <td><%= event.start_at %></td>
    <td><%= event.end_at %></td>
    <td><%= link_to 'Show', event %></td>
  </tr>
<% end %>


</table>


<br />


